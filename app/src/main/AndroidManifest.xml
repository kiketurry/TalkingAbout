<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.kiketurry.talkingabout">

    <queries>
        <package android:name="com.whatsapp" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalkingAbout">

        <activity
            android:name="es.kiketurry.talkingabout.ui.DistributiveActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ActivityFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.cats.splash.CatSplashActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.cats.CatsActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.calculator.CalculatorActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.bgg.BGGActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />

    </application>

</manifest>